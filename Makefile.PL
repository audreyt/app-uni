#!/usr/local/bin/perl
use utf8;
                                            # to pick up M:P:Au for now...
                                            use lib 'lib';
use inc::Module::Package 'Au:現代 0.01';
